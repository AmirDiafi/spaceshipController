import {StyleSheet} from 'react-native'
import {WIDTH} from '../../configs/constants'

const styles = StyleSheet.create({
  //position controller buttons
  buttonsContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    width: 150,
    height: 150,
  },
})

export default styles
